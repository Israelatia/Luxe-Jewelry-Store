apiVersion: v1
kind: Service
metadata:
  name: luxe-frontend
  namespace: demo-app
spec:
  selector:
    app: luxe-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
