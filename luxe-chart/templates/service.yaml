apiVersion: v1
kind: Service
metadata:
  name: {{ include "luxe-chart.fullname" . }}
  labels:
    {{- include "luxe-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "luxe-chart.selectorLabels" . | nindent 4 }}
