{{- if .Values.persistence.frontend.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "luxe-jewelry-store.fullname" . }}-frontend-pvc
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "luxe-jewelry-store.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  accessModes:
    - {{ .Values.persistence.frontend.accessMode | default "ReadWriteOnce" }}
  resources:
    requests:
      storage: {{ .Values.persistence.frontend.size | default "1Gi" }}
{{- end }}
