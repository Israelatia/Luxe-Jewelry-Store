# Use Nginx to serve the website
FROM nginx:alpine

# Copy your website files to the Nginx html folder
# Adjust "./" to the folder where your index.html is
COPY . /usr/share/nginx/html/

# Expose port 80
EXPOSE 80
